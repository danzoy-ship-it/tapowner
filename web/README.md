# web/

Next.js marketing site + signup + Stripe billing portal (Netflix pattern — all
purchase flows live here, never in the app).

**Scaffolded at Phase 5** (Auth + Stripe subscriptions). Placeholder until then
per the build order in `../TAPOWNER_BUILD.md`.
