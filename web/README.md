# web/

Next.js marketing site + signup + Stripe billing portal (Netflix pattern — all
purchase flows live here, never in the app).

Scaffolded (Next.js App Router, TypeScript, Tailwind) at Phase 5. Set
`NEXT_PUBLIC_API_BASE_URL` in `.env.local` (see `.env.local.example`) before
running `npm run dev`.
