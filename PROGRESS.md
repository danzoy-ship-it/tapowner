# TapOwner — Build Progress

One line per phase per the working rules in `TAPOWNER_BUILD.md` §10.
Technical decisions get noted here too (Claude Code decides technical, Frederick decides money + business facts).

## Phase log

- **2026-07-13 — UI redesign (Frederick-requested, pre-TestFlight): bottom tabs + push screens replace the single mega-sheet.** Frederick's audit request confirmed what the night's bug pattern showed: one bottom sheet was doing five jobs (property facts, trace CTA, trace results, the whole draft-email form, the whole save flow), which caused the overflow/notch/hidden-X/no-way-back symptoms. New architecture (React Navigation, native stack + bottom tabs): **Map / Pipeline / Account tabs**; parcel tap → slim `PropertyCard` (facts + one CTA); unlock → pushed `ContactScreen` (ranked phones + badges, emails, Draft Email, Save to Pipeline w/ note, Save to Contacts, Share Contact); Draft Email → pushed `DraftEmailScreen` (template list rows, tone chips, per-recipient checkboxes, sequential one-per-recipient composers); Pipeline tab = the CRM (filter chips → list → pushed detail with status/notes), with a Closer upsell state for prospectors; Account tab absorbs the old Settings (agent profile) + plan/usage card (tier, price from config, traces remaining, trial end) + legal links + data-broker notice slot + logout. Native back buttons everywhere via stack headers. State via `AppContext` (token/me/config/features/refreshMe/logout/showUpgrade) instead of prop drilling; `UpgradeSheet` is now a global overlay. Retired: ParcelSheet, SavedPropertiesScreen, SettingsScreen. Also this pass: Frederick's real logos landed (`logo-mark.png` → app icon, wordmark → expo-splash-screen config) replacing the generated placeholder. **Requires a new EAS dev build** (react-native-screens + react-native-safe-area-context are native) — the currently-installed dev client will crash on reload until it's installed; Metro restarted with cleared cache and the old app deliberately can't hot-load the new code. Typechecked clean first pass. On-device verification of the whole new flow = next session with Frederick.
- **2026-07-13 — Phase 10 code-side COMPLETE (events + compliance surfaces + store prep); remaining items are Frederick's App Store Connect / legal actions.** **Events (§9) verified live end-to-end:** new `logEvent` helper (never throws — metrics can't break requests); `signup_started` (checkout-session, keyed by email since the users row doesn't exist yet), `signup_completed` (checkout webhook), `trial_converted{tier}` + `upgrade_prospector_to_closer` (subscription-updated webhook, old-vs-new status/tier diff), `trace_purchased{charged_via,vendor_called}` + `trace_no_match` (trace route), `draft_created` (renamed from draft_generated, rate-limit query updated), and a new whitelisted client-events endpoint `POST /events` for `app_open` + `contact_saved` (fired from the app; fire-and-forget). Verified in production: app_open, contact_saved, signup_started, trace_purchased (via a real fresh trace on the Kingsland lake house — which also confirmed the new phone-ranking fields flow end-to-end on fresh vendor traces: rank/reachable/DNC all present and sorted), draft_created, and unknown-name rejection. The three webhook-fired events + trace_no_match ride the exact plumbing Phase 5b verified with signed webhook replays; they'll be observed live with the first TestFlight conversions — flagged, not glossed. `weekly_active` is a query over app_open, not an event. **Compliance surfaces:** tapowner.com/terms + /privacy drafted (DRAFT-banner until attorney review) — ToS §3 carries the appendix's five mandatory clauses (lawful use, no FCRA, own-your-outreach-compliance TCPA/CAN-SPAM, honor opt-outs, no harassment); privacy policy covers both audiences (subscribers + property owners) including a suppression-request path; signup now has the ONE required agree-checkbox linking both; web footer (new, server component) + app Settings both render the config-driven `data_broker_notice` slot — paste the SOS text into products.config after registration and it appears everywhere with zero deploys. **Store prep:** app display name "mobile"→"TapOwner" (slug untouched — changing it would orphan the EAS project), generated a minimal placeholder icon (blue field/white house/tap dot — flagged for replacement by a designed one), `APP_STORE.md` written with the exact privacy-label table (notably: Contacts = NOT collected under Apple's definition since we only write via the system form and never read; location = not-linked/not-tracked), the build doc's verbatim App Review notes, listing checklist, production build+submit commands, and the pre-launch legal gates with their sequencing (closed founding-agent TestFlight OK before the legal gate closes; PUBLIC launch is not). **Heads-up recorded: Resend must be set up before App Store submission** — Apple's reviewer needs a login that actually receives OTP codes (10-minute task, free tier). Remaining Phase 10 acceptance (TestFlight on 5+ founding agents' phones, ≥99% crash-free, submission sent) requires Frederick's App Store Connect actions per APP_STORE.md.
- **2026-07-13 — STATEWIDE LOAD STARTED + CAD enrichment formally authorized (Frederick: "absolutely... it definitely needs to be done").** Statewide: running `data/load_all_counties.py` (patched with `--skip-loaded` so the 5 metro counties aren't re-downloaded/re-loaded) against production for the remaining ~248 StratMap counties — overnight background job from the dev machine, transactional per county (safe to interrupt/resume via `--start-after`), downloads cached in `data/downloads/` (12GB cached so far, 793GB disk free), projected final DB ~9-10GB of the 30GB volume. Fits existing spend; no volume resize needed. **CAD enrichment** (real living sqft, beds/baths, stories, pool, casita/ADU, sale history via direct per-county CAD exports — the `cad_field_map` table from Phase 1 was built for exactly this) is now an authorized project, not just a wishlist item: plan is to pilot with Bexar CAD's public export after Phase 10 ships, then Harris/Dallas/Travis/Tarrant, since per-county formats differ (same adapter pattern the build doc §6 prescribes). Until it lands, AI drafts deliberately stay location-focused (see prompt rule from feedback round 2). Also this stretch: fixed address search being yanked back by location tracking (flyTo now disables follow-mode first; "My location" button re-enables), fixed on-map address labels never having rendered at all (symbol layer needed `text-font: Noto Sans Regular` — the layer's default font stack 404s on OpenFreeMap's glyph server; this was the MapLibre ERROR chip Frederick screenshotted), labels now start at z17, initial camera starts over San Antonio instead of the 0,0 world view, and documented that dev-mode Fast Refresh reloads (from my file saves) were Frederick's "app randomly restarts while I type" reports — not an app bug, and production builds don't do it.
- **2026-07-13 — Frederick's field-testing feedback round 2 (4 items) + two standing work items logged.** (1) **Phone ranking:** BatchData V3 returns per-phone `rank` (their confidence, 1=best), `reachable`, and `tested` — we'd been discarding all three. Provider now captures them and sorts (verified-reachable first, then vendor rank, mobile over landline); app shows the top 2 with a "Show N more numbers" expander and a green "Verified" badge on vendor-verified numbers. **Note: already-cached traces (his house, Borjas) serve their stored payload, so they keep the old unranked order until their 90-day cache expires — only fresh vendor traces get ranking fields.** (2) **AI drafts padding thin data** ("strong foundation" filler): root cause is the StratMap schema gap (only year-built/lot-size/assessed-value exist for ANY parcel — documented Phase 1 limitation); prompt now explicitly forbids padding sparse details and redirects to location/neighborhood/ask instead. **STANDING WORK ITEM (Frederick: "I don't wanna forget"): per-county CAD-export enrichment** — sqft/beds/baths/pool/stories/casita etc. via direct CAD exports (the build doc §6 footnote's "quarterly big-metro refresh" plan, `cad_field_map` table already exists for it). Until then drafts stay location-focused. (3) **Statewide coverage question answered with data:** 5 counties (4.51M parcels) = 3.3GB actual DB size → all-Texas (~12M parcels) projects to ~9GB — fits the existing 30GB volume with room to spare, so NO vendor API and NO volume spend needed. StratMap is already the single statewide source (253/254 counties, one TxGIO S3 bucket, one annual refresh — not "hundreds of counties that could disconnect"); the Phase 1 orchestrator (`data/load_all_counties.py`) just needs to run for the remaining ~248 counties. Offered to run it overnight; awaiting Frederick's go. (4) **"Save Property froze the screen":** most likely cause is my own API restarts during Phase 9 testing landing exactly in his tap window — RN fetch has no default timeout, so a request hitting a restarting server hangs indefinitely and reads as a frozen UI. Hardened regardless: every app request now goes through a 15s AbortController timeout with a clear "Request timed out" error (api.ts + auth.ts), and the keyboard dismisses on save. Asked him to retest; if it still freezes with the API stable, it's something else and needs real diagnosis, not this guess.
- **2026-07-13 — Phase 9 — PASSED ✅ (server criteria fully verified live; mobile paywall/expiry screens are JS-only, no new build needed).** `GET /config` (whitelisted from products.config — commission rates and internal knobs deliberately excluded — plus the draft template/tone list, an automatic sha256-derived `version`, 60s in-memory cache + Cache-Control). Config-driven feature gates: per-tier `features` flags added to products.config; new `api/src/lib/entitlements.ts` (`requireFeature`) replaces every hardcoded `tier === 'closer'` check in draft/savedProperties routes — 403 with `upgrade_required` + `feature` for a tier lacking the flag, 402 with `subscription_inactive` for a non-trialing/active subscription. **Acceptance headline proven live with zero deploys: flipped `closer.features.crm` to false in the DB → the same closer request went 200 → 403 upgrade_required → flipped back → 200.** Status enforcement also closes the Phase 6 flagged bug (canceled subs could still consume traces) — and fixes a worse latent one found while wiring it: `/trace` used to check the subscription only AFTER the vendor call, so a subscription-less user could cost real BatchData spend before being rejected; the check now happens first (free re-views stay accessible on a lapsed sub — the user already paid for that data). Frederick's test account flipped from stale `canceled` to honest `trialing` so his on-device testing survives enforcement. Trial-expiry flow: `EmailProvider` gained a generic `sendEmail`; a 6-hourly sweep (`api/src/jobs/trialReminder.ts`, first run 30s after boot) emails accounts whose trial ends within `trial_reminder_days_before` (3) days, deduped via a `trial_reminder_sent` event per subscription — **verified live end-to-end:** QA account with trial ending tomorrow got the correct email through the console provider on restart, and the sweep query returns zero candidates afterwards (dedupe proof). Day-27 **push** notification deferred: needs expo-notifications (native module) + an APNs key — batch with the next EAS build; the email half satisfies "day-27 reminder" for now and Resend slots in with zero code change. Canceled-sub 402s verified on both /trace and /saved-properties. Mobile: config fetched at launch (bundled fallback if offline), trace-price label and draft templates/tones now config-driven; prospectors now SEE Draft Email/Save/Contacts buttons and tapping opens the UpgradeSheet (pitch built from config prices, "Manage your plan at tapowner.com" text per §3's no-purchase-link decision — deliberately not tappable); ExpiryScreen (two tier cards from config + re-check + logout) replaces the map when /me reports a non-usable status. **Also this stretch, from Frederick's live on-device testing:** Phases 7+8 confirmed working on his phone (trace on his own house: real phones/emails, DNC badges visibly rendering — closing Phase 6's last visual-confirmation item); "Save to Contacts" originally triggered iOS 18's alarming "share all 1,397 contacts" permission prompt — replaced `requestPermissionsAsync`+`addContactAsync` with `presentFormAsync` (native prefilled New Contact form, user reviews and taps Done, NO contacts permission needed at all — also better App Review optics); added a "Send to" recipient picker in the draft flow at his request (multiple traced emails = tap-to-toggle chips, defaults to the first); fixed the account-bar text collision. Google Geocoding API enabled by Frederick mid-session — `/geocode` verified live against both his real addresses; address search is now fully functional end-to-end. Next: Phase 10 (polish, compliance, TestFlight, App Store) — the last phase.
- **2026-07-13 — Out-of-plan addition: address search + on-map labels (Frederick's idea, not in TAPOWNER_BUILD.md's 10 phases).** Frederick wanted to scope neighborhoods remotely (type an address, map recenters) without being physically there, and better address/house-number visibility on the map itself (currently only shown after tapping a parcel). Built both: `GET /geocode?address=` (server-side proxy to Google's Geocoding API, key never ships to the app, biased to `administrative_area:TX` since that's the only state loaded) + a search bar in the mobile app that flies the MapLibre camera to the result; and a zoom-gated (`minzoom=18`) MapLibre symbol layer showing a short address label per parcel, using a `label` field added to the existing tile query (`trim(split_part(situs_address, ',', 1))` -- no schema change needed, just a computed column in the MVT query, since situs_address was already in every tile). **Blocked on Frederick's Google Cloud project:** the `GOOGLE_PLACES_API_KEY` he provided returns `REQUEST_DENIED` — the Geocoding API specifically isn't enabled on that project yet (Places API and Geocoding API are separate toggles under Google Maps Platform even though they share a key/project). Added proper error logging for this (previously any non-OK Google status was flattened into a generic "no match" 404, which would have made this genuinely hard to diagnose later) rather than leave it silently swallowed. Waiting on Frederick to enable the Geocoding API in Google Cloud Console, then this is ready to test.
- **2026-07-13 — Bulk select/trace/email discussed, deferred (not built).** Frederick asked about drawing a lasso around a neighborhood, bulk-tracing every parcel inside it, then emailing all of them -- explicitly for the roofing vertical's hail-damage-area use case, and flagged himself that "how do you send 50 emails" isn't solved. Recommendation given: bulk select+trace is buildable and low-risk (PostGIS is made for this), but bulk *auto-send* email is a real compliance problem, not just a UX one -- Phase 7 deliberately opens Mail prefilled rather than sending from TapOwner's own infrastructure specifically to keep a human reviewing/sending every message and keep TapOwner out of CAN-SPAM/deliverability territory. Proposed approach if/when built: bulk select + bulk trace with an upfront cost-confirmation step (real per-property vendor spend), then a fast review-queue UI (swipe through drafted emails one at a time, still a human tap-to-send each time) rather than true background bulk-send. Not scheduled; revisit when the roofing vertical (or a real-estate customer's actual demand) makes it a priority.
- **2026-07-13 — Phase 8 — PASSED ✅ (backend fully verified live; mobile UI needs the pending EAS dev-client build, same as Phase 7 -- batch it with that one, don't rebuild twice).** `saved_properties` (status enum: new/contacted/follow_up/appointment/listed/dead, unique per user+parcel) + `notes` (one-to-many, own table per the build doc's §5 schema, not a single text column -- supports a running timeline, not just one description) added exactly as documented, no schema surprises this time. API: `POST /saved-properties` (Closer gate + must-be-traced-first gate, idempotent on re-save via `ON CONFLICT ... DO UPDATE SET status = status` so tapping Save twice never duplicates or resets progress), `GET /saved-properties` (list, joined with parcel fields, optional `?status=` filter, includes note_count/latest_note for the list view), `GET /saved-properties/:id` (full detail + all notes), `PATCH /saved-properties/:id` (status change, validated against the enum), `POST /saved-properties/:id/notes`. All ownership-checked (a saved property can only be read/modified by the user who saved it). Verified live end-to-end: save-with-note, status change, add a second note, detail view returns both notes newest-first, invalid status rejected (400), re-save idempotency confirmed (still exactly 1 row, status preserved), filter-by-status confirmed. Mobile: new `SavedPropertiesScreen.tsx` (status-filter chip row, list, tap-through to a detail view with a status picker and note timeline, opened from a new "Saved" link in the account bar); `ParcelSheet.tsx` gained "Save Property" (Closer-gated, optional note field, idempotent re-tap), "Save to Contacts" (Closer-gated, writes name/phones/emails via `expo-contacts` into the device's real Contacts app), and "Export Contact (vCard)" (available to **both tiers** per the build doc's explicit carve-out -- implemented as a plain client-side `.vcf` file write + native share sheet, no backend call at all, so it works the same for Prospector and Closer). **Real platform constraint hit and worked around:** iOS restricts writing to a Contact's `note` field behind a special Apple entitlement (`com.apple.developer.contacts.notes`) that's essentially never granted to normal apps (anti-spyware/parental-control abuse prevention) -- attempting to set it would throw at runtime regardless of dev vs. App Store build. Rather than silently drop the requested "notes into the contact" behavior, the property address and any note text go into the Contact's Company field instead, which has no such restriction and is genuinely visible on the contact card -- a real deviation from the build doc's literal wording, flagged here rather than silently shipped. **Also found: `expo-file-system` shipped a breaking API rewrite in the SDK 54 version** (old `FileSystem.writeAsStringAsync`/`cacheDirectory`/`EncodingType` don't exist on the default import anymore) -- caught immediately by `tsc`, fixed using the new synchronous `File`/`Paths` class API (`new File(Paths.cache, name).create({overwrite:true}); file.write(text)`) instead of reaching for the `expo-file-system/legacy` shim. Two more native modules added this phase (`expo-contacts`, `expo-file-system`) -- both need the same pending EAS dev-client rebuild Phase 7 is waiting on; no need for Frederick to run a second build, one rebuild after this session covers both phases. Next: Phase 9 (config service, gates, paywall states) or Phase 10, or wait for the on-device test of Phases 7+8 together once the build lands.
- **2026-07-13 — Phase 7 — PASSED ✅ (backend fully verified live; mobile UI needs a new EAS dev-client build before on-device testing).** `POST /draft` built: 5 launch templates (`api/src/draft/templates.ts`) with per-template goals fed into a prompt built from the agent's profile + traced parcel's real details; calls Anthropic's `claude-haiku-4-5-20251001` server-side only (key never ships to the app). Gates, in order: Closer tier required (403 for Prospector, verified with a throwaway test account), the parcel must already be traced by this user (403 otherwise, verified), a completed agent profile (name required, 400 otherwise), then a config-driven 30/day rate limit (`draft_rate_limit_per_day`, counted off the existing `events` table rather than a new table). New `PUT /me/profile` endpoint saves agent_profile (name, brokerage, phone) onto the existing `users.agent_profile` jsonb column -- no new tables needed for this phase at all, matching the build doc's schema (§5) exactly as documented. Token cost computed from the real Anthropic usage response and logged per-draft as an `events` row (name=`draft_generated`) alongside template/tone/parcel for analytics -- five real test drafts against a real traced property came in at $0.0011-0.0014 each, comfortably under the <$0.01 acceptance bar. Mobile: new `SettingsScreen.tsx` (agent profile form, opened from an account-bar "Settings" link in `App.tsx`), `ParcelSheet.tsx` gained a template/tone picker and "Draft Email" button (Closer-only; shows a locked hint on Prospector) that calls `/draft` then opens `expo-mail-composer` prefilled, addressed to the traced owner's email if one was found. **Real bug found+fixed during testing:** despite explicit prompt instructions not to, Claude Haiku reliably wrapped its JSON response in a ` ```json ` code fence, which broke `JSON.parse` and silently fell through to a fallback path (raw fenced text as the email body, template label as the subject) -- fixed with a defensive fence-strip before parsing, confirmed against a real captured Anthropic response in isolation before redeploying. **Real quality issue found+fixed:** the model twice misspelled the agent's actual brokerage name ("Salasers" -> "Salesers") even when given the exact string verbatim -- rather than trying to prompt-engineer around an LLM's unreliable handling of an unusual proper noun, moved the sign-off (name/brokerage/phone) out of the model's output entirely and appended it deterministically in code; the model now only writes the message body itself. **Gotcha: `railway redeploy` silently rolled back to an older build** mid-session (matches the existing documented gotcha that it restarts the *previous* artifact, not the current source) -- cost about 10 minutes chasing a phantom "fence fix isn't working" bug that was actually just stale-deployment confusion; resolved by always using `railway up --service api --ci` for a real rebuild and confirming via `railway status --json`'s `latestDeployment.id/status` (matched against the deploy command's own printed deployment id) before retesting, rather than trusting the CLI's own build-log stream (which failed to attach on every single deploy this session for unrelated reasons but did not indicate real failures). **Still needed before the mobile side can be tested on-device: a fresh EAS dev-client build** -- `expo-mail-composer` has native code, same as `expo-secure-store`/maplibre earlier in the project. Next: Frederick runs `eas build --profile development --platform ios` when ready; then full on-device acceptance test (tap Draft Email -> pick template/tone -> Mail opens prefilled with correct property/owner details).
- **2026-07-13 — Gotcha: dev-client couldn't reach Metro on Frederick's home network, root cause never fully isolated.** After the Phase 6 on-device test, his phone's Expo dev-client couldn't connect to this PC's Metro server (`192.168.50.65:8081`) — "No development servers found" plus an instant "Failed to connect" on manual entry. Ruled out, in order: wrong Wi-Fi/SSID (same network confirmed), Windows Firewall (Private profile, explicit Node.js allow rule, no block rules, interface/remote-address unrestricted — all confirmed via read-only checks), iOS Local Network permission (already granted). Real signal: Safari on the phone failed identically ("server can't be found") to the same address, proving it wasn't Expo-specific. Tried routing over Tailscale (already installed on this PC) as a bypass — network layer worked (ping succeeded, Safari over the Tailscale IP got a real response) but the native dev-client app itself failed instantly, consistent with iOS App Transport Security not treating Tailscale's CGNAT range (100.64.0.0/10) as "local network" the way it does RFC1918 addresses -- a native rebuild would be needed to fix that path, not worth it here. Checked the router (ASUS ZenWiFi Pro ET12 mesh): AP Isolation was already off, DoS protection was already off, no custom firewall rules existed. Frederick toggled AiProtection's Two-Way IPS off (it showed 0 logged blocks throughout) and the direct LAN connection started working right after -- but he rightly pushed back that a zero-event counter is weak evidence of causation, and other things changed around the same time (this PC's Metro process had been killed/restarted several times during troubleshooting, and his phone's "Roaming assistant" setting could have refreshed a stale mesh/ARP entry independent of any router toggle). **Honest conclusion: the fix worked, the exact cause is not established.** If this recurs: check Two-Way IPS first (cheap to toggle), but don't assume it's the whole story -- also try a fresh Metro restart and consider mesh roaming/ARP staleness before spending more time on router settings.
- **2026-07-13 — Phase 6 — PASSED ✅.** Built while Frederick slept, per his go-ahead to pick BatchData V3 and keep working. `TraceProvider` interface (`api/src/trace/provider.ts`) + `BatchDataProvider` (`api/src/trace/batchdata.ts`, calls the V3 endpoint, aggregates phones/emails across all returned persons, matching the bakeoff's proven multi-owner approach) + factory (`api/src/trace/index.ts`, env-driven via `TRACE_PROVIDER`/`BATCHDATA_API_TOKEN`). Schema: `trace_results` (vendor payload cache, keyed by parcel + sha256 of owner_name so a change of ownership invalidates the cache, 90-day TTL from `products.config`) and `user_traces` (per-user unlock record, unique on user+parcel so a user's own re-view is always free). Billing: `chargeForTrace` consumes a Closer's `included_traces_remaining` first, otherwise reports Stripe metered usage via the Billing Meter (`trace_used` event) — never a bare $0.29 charge. `POST /trace/:parcelId` wired into `api/src/index.ts`; extracted `getStripe()`/`getProductConfig()` into `api/src/lib/` so `billing.ts` and `trace.ts` share one implementation instead of duplicating. Railway `TRACE_PROVIDER` flipped from the stale bakeoff value `tracerfy` to `batchdata` (the `BATCHDATA_API_TOKEN` real token was already set from the bakeoff, no new secret needed); deployed via `railway up api --path-as-root --service api --ci`. **Full acceptance test run against the live production API and DB, using a real matched address pulled from the bakeoff's own known-good set (parcel 199178, Bexar) so the vendor call would actually hit — not synthetic data:** (1) fresh trace → `matched:true`, 2 real phones + 3 real emails returned, `included_traces_remaining` 10→9, log line `vendorCalled=true chargedVia="included"`; (2) same user re-viewing the same parcel → `freeReview:true`, identical cached payload, no new charge, no new `user_traces` row; (3) a second, independent user (created via the real OTP flow, given a throwaway `closer` subscription with 1 included trace via direct SQL — no fabricated Stripe objects touched anything real) traced the *same* parcel → charged (`included_traces_remaining` 1→0) but log proof confirms `vendorCalled=false` — the `trace_results` cache served it, no second BatchData spend; (4) response phones carry `dnc`/`tcpa` booleans end-to-end (both false for this address; the mobile UI's badge is a plain conditional render on those booleans, so correctness doesn't depend on this specific address having a true flag). Throwaway QA user/subscription/trace rows deleted after the test; the real matched cache row (parcel 199178) and Frederick's own test account's real trace (`danzoy@gmail.com`, now on 9/10 included traces) were left in place intentionally. Mobile: `ParcelSheet.tsx`'s "Get Contact Info — $0.29" button is no longer disabled — calls the new endpoint, renders phone/email rows with DNC/TCPA badges, shows a "no charge" note on free re-views, shows the vendor's no-match message without ever implying a charge happened. `App.tsx` now keeps the session token in state (previously write-only to SecureStore) so `ParcelSheet` can make authenticated calls. Both `api/` and `mobile/` typecheck clean (`npx tsc --noEmit`). **On-device confirmation done: Frederick tapped a real property on his physical iPhone (his mother-in-law's house) and the unlocked card rendered 1 mobile + 1 landline + 5 emails — all independently confirmed correct by Frederick, who knows the ground truth.** Real accuracy proof, not just a rendering check. Phase 6 acceptance test is now fully closed, on-device and production-verified end to end. **Found, not fixed (flagging, not silently scope-creeping into it):** `danzoy@gmail.com`'s subscription row has `status:"canceled"` yet `tier:"closer"` with traces remaining — `chargeForTrace`/the trace route pick the latest subscription row by `created_at` and never check `status`, so a lapsed/canceled subscription can still consume included traces or get metered today. Likely harmless leftover from Phase 5's cancellation-webhook test, but worth a real status check before Phase 10 launch hardening. Next: Phase 7 per the build doc's phase order, or whatever Frederick prioritizes on waking.
- **2026-07-13 — BatchData V3 beats V1 at the same price -- use V3 for the real Phase 6 integration.** V3 returns up to 3 distinct persons per property (e.g. both co-owners) billed identically to V1 (per matched property, not per person). Same 100-address set: 89% phone match, 80% email match (both ~same as V1), but 28% of hits returned >1 person, and checking against all returned persons (not just one) gives 90.3% name-overlap on multi-owner properties -- resolves most of the "multi-owner mismatch" noise in the V1/Tracerfy accuracy numbers. No reason to use V1 over V3 given identical pricing. Full 3-way comparison: `data/bakeoff_report_comparison.md`. Bug found+fixed: V3's response `data[]` array has explicit `null` entries for no-match rows (V1 omits them silently) -- crashed on first run, fixed with null-guards (`(x.get(k) or {})` pattern instead of `x.get(k, {})`, since the latter only substitutes the default when the key is *absent*, not when its value is explicitly `null`). **BatchData MCP server exists at https://mcp.batchdata.com** (Frederick flagged it) -- not used for anything, would only help ad-hoc dev-time queries, doesn't change the production TraceProvider integration which calls their REST API (V3) directly regardless. **Phase 6 bakeoff is now fully done, both vendors compared, decision is Frederick's** (cost + free co-owner contacts both favor BatchData V3, but he decides/signs).
- **2026-07-13 — Phase 6 bakeoff (2/2) COMPLETE: BatchData — 88% phone match, cheaper than Tracerfy.** Same 100-address test set. Frederick clarified BatchData's sandbox token returns mock/fake data (confirmed empirically -- Lorem-ipsum names, nonsense addresses) and authorized using the real token for actual bakeoff testing, which is within the compliance appendix's "internal evaluation" carve-out. Real per-hit cost confirmed via account-balance diff: $0.07/hit (cheaper than Tracerfy's $0.10/hit), misses free (confirmed in their own API docs: "matchCount indicates the number of requests items that will be billed"). Results: 90% hit rate, 88% phone match rate (clears the 70% bar), 73.2% clean-single-owner name accuracy -- nearly identical profile to Tracerfy (90%/90%/73.8%), as expected since both vendors likely license overlapping underlying data. **Real bug found+fixed during testing:** our own situs_address data has a double-space artifact between house number and street name (leftover CAD formatting from Phase 1), which didn't match BatchData's single-space-normalized echoed address in my post-hoc address-matching key -- silently misattributed 59 of 91 real matches as "no match" on the first run (reported 32% before the fix, vs the true 90%). Fixed by normalizing whitespace before comparing. Cost note: the buggy first run still incurred real billing (BatchData bills on its own match determination, not on how my script parses the response after the fact) -- real total spent testing BatchData across both runs was ~$12.67, not the $6.30 the corrected run alone shows. Full comparison: `data/bakeoff_report_comparison.md`. **Both vendors clear the gate; this is now Frederick's pick to make** (cost is the only real differentiator -- BatchData ~30% cheaper per hit, ~13% better margin at the $0.29 price point).
- **2026-07-13 — Phase 6 bakeoff (vendor 1/2): Tracerfy — 90% phone match rate, clears the ≥70% bar.** Ran 100 real Bexar/Dallas/Harris addresses (own loaded parcel data) through Tracerfy's sync `/v1/api/trace/lookup/` endpoint. 92% hit rate, 90% phone match rate, $9.20 total cost ($0.10/hit -- see pricing correction below), ~1.4s avg response. Full report: `data/bakeoff_report_tracerfy.md`. **Pricing correction found live:** Tracerfy's pricing page advertises $0.02/hit, but that's specific to their bulk-CSV batch endpoint -- the sync/instant endpoint (the only one that fits TapOwner's real "tap and wait <2s" UX) is a flat 5 credits ($0.10/hit) regardless of whether owner name is supplied, confirmed via two live test calls. Still within the build doc's own $0.15/match assumption, just not the advertised bargain. Name-accuracy nuance (not a pass/fail signal, doc's gate is phone match rate): a naive name-compare looked weak (59.8%) until categorized -- 19 hits were LLC/business-owned (skip trace can't return "the LLC's phone," returns an associate instead, expected), 31 were multi-owner records (either co-owner is a correct match), leaving 42 clean single-owner cases at 73.8% ground-truth match (remainder could be genuine misses or our StratMap snapshot being staler than Tracerfy's live data -- not fully separable without deeper research). Travis/Tarrant excluded from this round's sample -- a slow, unindexed query on those two counties specifically (not a vendor issue); backfill if a full 5-county sample is wanted later. **Still need BatchData run through the same 100-address test before any vendor pick or contract signing** (build doc requires comparing both before deciding).
- **2026-07-13 — Pre-launch legal/compliance gate appendix added to TAPOWNER_BUILD.md** (before the roofing-vertical appendix). Hard gate: no public launch until vendor redistribution addendum + ToS + TX Data Broker Act registration + insurance + attorney review are done. **Two implementation hooks for future phases, logged here so they don't get lost:** (1) Phase 10's ToS/privacy-policy draft must cover the five listed user-terms clauses (lawful-use only, no FCRA uses, user owns outreach compliance, honor opt-outs, no harassment use) as one signup checkbox, not a disclaimer wall; (2) web footer + app settings need a config-driven notice slot for the TX data-broker conspicuous notice (text comes from config once registration happens, not hardcoded). Frederick separately confirmed: the Phase 6 bakeoff (100-address test) is approved under standard self-serve vendor terms now — the reseller addendum only gates real end users seeing traced data, not the internal evaluation/testing itself. Nothing to build yet; noted for Phase 6 and Phase 10 planning.
- **2026-07-12 — Phase 0 — STARTED** — Monorepo scaffolded (mobile/, api/, web/, data/), TypeScript configs, Fastify hello-world with `GET /health`, Expo blank app (TypeScript). Railway deploy pending Frederick's account (costs money — approval gate).
- **2026-07-12 — Phase 0 — infra complete** — API deployed on Railway (`https://api-production-7d11.up.railway.app/health` → 200, 228ms). PostGIS db service live (postgis/postgis:16-3.5, volume attached, extensions loaded, DATABASE_URL wired to api over private network). Acceptance: deployed /health ✅; iPhone Expo Go render pending Frederick's rescan after SDK 54 pin.
- **2026-07-12 — Phase 0 — PASSED ✅** — Both acceptance criteria green: deployed `GET /health` → 200; blank app rendered on Frederick's physical iPhone via Expo Go (SDK 54). Next: Phase 1 (StratMap → PostGIS pipeline).

## Technical decisions

- **2026-07-12 — No npm workspaces.** Each package (`api/`, `mobile/`, `web/`, `data/`) manages its own dependencies. Expo's Metro bundler is friction-prone with hoisted node_modules; workspaces buy nothing at this scale.
- **2026-07-12 — Local Node is v24** (satisfies `engines >=22`). Railway deploy will pin Node 22 per the architecture doc.
- **2026-07-12 — GitHub repo `danzoy-ship-it/tapowner` pre-existed as PUBLIC.** Flagged to Frederick: consider making it private (repo Settings → Change visibility) — the build doc contains the full pricing model and margin math.
- **2026-07-12 — `web/` and `data/` are README placeholders** until their phases (5 and 1 respectively) — Phase 0 scope is API hello-world + Expo blank app only.
- **2026-07-12 — Railway deploys are GitHub-sourced, not CLI-uploaded.** `railway up` from this Windows machine dies instantly on the Metal builder with no logs (twice with Railpack, once with Dockerfile). Connecting the service source to the GitHub repo builds fine — and gives push-to-deploy on `main`. Root `Dockerfile` builds `api/` (context = repo root); when `web/` ships it gets its own service with a root-directory setting.
- **2026-07-12 — db service: `PGDATA=/var/lib/postgresql/data/pgdata`.** Railway volumes mount with `lost+found`, which makes initdb refuse the mount root; pointing PGDATA one level down is the standard fix.
- **2026-07-12 — StratMap common schema does not carry living area, beds/baths, stories, pool/garage indicator, or sale price/date for ANY county** — confirmed against the real 2025 Bexar file (709,541 features): these columns simply don't exist in the .dbf. TxGIO's own description confirms why: "StratMap Land Parcels are intended to be used as basemap and cartographic data and are not for detailed analysis." Per TAPOWNER_BUILD.md §6's own footnote ("later we refresh big-metro counties quarterly from CAD exports directly — documented future task, not v1"), v1 proceeds StratMap-only; those columns are null for every parcel until a direct-CAD-export source is ingested (not v1). Flagging because Phase 4's UI promises these details per-property — that promise can't be met for ANY property under the current v1 data source, not just per-county coverage gaps. Business/roadmap conversation, not a blocker for Phase 1 itself.
- **2026-07-12 — StratMap is public, unauthenticated, CC0-licensed, one zip per county** — found via the DataHub's S3 warehouse (`s3://tnris-data-warehouse/LCD/collection/stratmap-2025-land-parcels/items/{shp,fgdb}/`), 253 of 254 counties present. No TxGIO account/token needed. Harris County ships as two shapefiles (east/west split — its dbf would exceed size limits otherwise); loader handles multi-shapefile counties.
- **2026-07-12 — Phase 1 — volume resized (Frederick approved spend), 5 metros loading.** Railway `db-volume` resized 500MB → 30GB. Bexar (709,541), Harris (1,523,641), Dallas (694,160) committed clean. Full 253-county list saved to `data/counties_2025.json`; statewide orchestrator written (`data/load_all_counties.py`) but not run yet — will need another volume check before a full statewide load (metros running larger than first estimated; 30GB may not cover all 253 counties).
- **2026-07-12 — Phase 2 — PASSED ✅ on production.** Deployed and verified against the live URL (not just localhost): all 5 counties (Bexar/Dallas/Harris/Tarrant/Travis) return correct owners via `GET /parcels/at`; tile endpoint returns valid MVT (200, 38,023 bytes), byte-identical on repeat; production logs show `tile cache miss` (19ms) then `tile cache hit` (no DB round trip) for the same tile key — cache proof confirmed on the deployed instance. Next: Phase 3 (App shell: map + GPS).
- **2026-07-13 — Phase 4 — PASSED ✅.** Verified on Frederick's physical iPhone: tapped his own house (2807 Stokely Hl) and neighbor's (2803 Stokely Hl), both returned correct owner/year-built/lot-size/assessed-value matching the Phase 1 database spot-check exactly (including the "STOKEY" mailing-address typo, confirming data isn't being silently cleaned). Highlight fill correctly targets only the tapped parcel. sqft/beds/pool/last-sale-price cleanly omitted (StratMap gap, not a bug). Disabled "Get Contact Info — $0.29 / Coming in beta" button renders correctly. Non-bug found while testing: panning to Austin (Williamson County) and Kingsland (Llano County) shows no parcel lines/taps -- confirmed via production logs (fast, clean, zero-result tile queries, no errors/rate-limits) that this is simply outside the 5 loaded counties, not a defect. Next: Phase 5 (Auth + Stripe subscriptions + app login).
- **2026-07-13 — Phase 3 — PASSED ✅.** Verified on Frederick's physical iPhone via the custom EAS dev-client build: app opens centered on real GPS location (blue dot confirmed in Shavano Park, San Antonio); parcel boundary lines render correctly at street-level zoom and correctly do NOT render at city-level zoom (minzoom=16 gate working); pan reported "very nice and smooth" (60fps target met, no stutter). Gotcha along the way: a leftover Metro process from earlier in the day was still squatting on port 8081 in non-dev-client mode, causing the fresh `--dev-client` server to silently skip startup ("Use port 8082 instead?" prompt with no TTY to answer) -- killed the stale process, clean restart fixed it. Also: iOS "Developer Mode" must be manually enabled in Settings > Privacy & Security before any non-App-Store build will launch -- one-time device setting, not a recurring step. Next: Phase 4 (Tap-to-owner -- the product moment).
- **2026-07-13 — Phase 5b — PASSED ✅.** Built partners/referrals/commission_ledger schema, admin partner creation (POST /partners, gated by ADMIN_SECRET -- a stopgap until a real admin-role system exists), public code lookup + QR generation, referral-click tracking, checkout-session referral attribution (self-referral rejected, 60-day attribution window authoritatively enforced server-side using the client-sent timestamp, not just client-side UX), a reusable 100%-off-once Stripe coupon for user_referral/founding_agent free-month rewards, and full invoice.paid/charge.refunded webhook handling for commission computation. web/: /r/[code] landing (stores attribution, tracks click, redirects to signup), /partner dashboard (OTP login, stats). Extensively tested using Stripe's API-level test payment method (pm_card_visa) and manually-signed webhook replays -- never entered payment info into any UI field. Verified: affiliate commission = round(revenue * rate) exactly (25% of $19.99 = $5.00); idempotent on stripe_event_id (duplicate delivery does not double-commission); user_referral credits the referrer's own Stripe balance by their own tier's price (not hardcoded); founding_agent correctly sets users.lifetime_closer=true; dashboard numbers match the ledger; QR endpoint returns a valid 512x512 PNG; /r/[code] redirect + attribution storage + click tracking + partner dashboard all confirmed working in an actual browser. **Real bug found+fixed during testing:** the refund-clawback loop's idempotency key was derived from referral_id, which collides when a referral has multiple positive ledger rows for the same invoice (an edge case my own duplicate-delivery test happened to construct) -- silently dropped the second clawback via ON CONFLICT. Fixed by keying the clawback off each original ledger row's own unique id instead. Next: Phase 6 (skip trace) -- needs Frederick to fund vendor sandbox credits before the bakeoff can start.
- **2026-07-13 — Phase 5 — PASSED ✅ (full acceptance test, real devices).** Frederick provided Stripe test-mode keys; created Prospector ($9.99/mo), Closer ($19.99/mo), and a metered Trace price ($0.29/unit, backed by a Stripe Billing Meter -- newer Stripe API versions require this, `aggregate_usage` is deprecated) via the API directly rather than the dashboard. Created a production webhook endpoint (checkout.session.completed, customer.subscription.updated/deleted). Deployed `web/` to Railway as its own service (was blocked by two real bugs, both found+fixed: (1) `railway up` from inside a subdirectory picked up the wrong Dockerfile -- root's api Dockerfile got deployed to the web service; fixed with `railway up web --path-as-root --service web` run from repo root, which correctly scopes the build context; (2) Dockerfile hardcoded port 3000, but Railway injects its own $PORT -- 502 until fixed with `next start -p ${PORT:-3000}`). Full real-device test: signed up on phone browser (danzoy@gmail.com) -> OTP -> real Stripe Checkout -> test card 4242... -> webhook fired -> subscriptions row correct (tier=closer, status=trialing, trial_ends_at=+30d, included_traces_remaining=10) -> logged into the mobile dev-client app with the same email -> /me on-device confirmed tier=closer -> canceled the test subscription via Stripe API -> webhook updated status=canceled in ~5s (well under the 1-minute bar). Real user data (danzoy@gmail.com) left in place intentionally, now canceled -- not test-throwaway data. Next: Phase 5b (referral/commission system) or Phase 6 (skip trace) per Frederick's priority.
- **2026-07-13 — Mobile login screen + session persistence built.** LoginScreen.tsx (email -> OTP, mirrors the web flow), auth.ts (expo-secure-store for JWT persistence, requestOtp/verifyOtp/fetchMe), App.tsx now gates the map behind auth (checks stored token on launch, validates via /me, shows LoginScreen if absent/invalid). Small account bar added showing email + tier (satisfies "tier awareness from /me") with a logout action. expo-secure-store has native code -> triggered a new EAS dev build; credentials reused from the first build (no Apple sign-in needed this time -- confirms that setup was truly one-time, not per-build). Build running in background.
- **2026-07-13 — web/ signup flow built and browser-tested end-to-end.** Next.js 16 (App Router, Turbopack default, Tailwind) scaffolded: landing page, /signup (email -> OTP -> Stripe checkout redirect), /welcome. Verified in an actual browser (not just curl) per the "test in browser before reporting complete" rule -- and it caught a real bug curl couldn't: the API had no CORS headers, so the browser silently blocked every request from the web app with a generic "Failed to fetch" (curl doesn't enforce CORS, so this was invisible until real browser testing). Fixed with @fastify/cors, allowlisting WEB_BASE_URL + local dev ports. After the fix: full flow verified live -- email submitted, OTP logged server-side and entered, verified, correctly proceeded to checkout-session call, which correctly surfaced "Billing not configured yet" (503, not a crash) since Stripe isn't set up. That's the real, expected stopping point until Frederick creates a Stripe account. Next: mobile login screen (doesn't need Stripe either).
- **2026-07-13 — Phase 5 (auth core) — built and verified locally.** Schema: products/users/subscriptions/otp_codes/events (§5), with a real UNIQUE index on subscriptions.stripe_subscription_id (needed for the webhook's ON CONFLICT upsert -- caught before deploying, not after). Email-OTP + JWT auth: POST /auth/otp/request, POST /auth/otp/verify, GET /me -- all tested end-to-end against the live Railway DB (request -> logged code -> verify -> JWT -> /me returned correct user, tier=null since no subscription yet). Verified: wrong code rejected, replay of a consumed code rejected, missing bearer token rejected. Email delivery is interface-first (EmailProvider): ConsoleEmailProvider (logs the code -- active now, no vendor account yet) and ResendEmailProvider (swap-in once Frederick creates a Resend account, zero code changes elsewhere). Billing routes (Stripe checkout session + webhook) written and gracefully return 503 "Billing not configured yet" when STRIPE_SECRET_KEY is unset, rather than crashing -- verified. JWT_SECRET generated and set on Railway. Still needed from Frederick: Stripe account (free, test mode) and Resend account (free) before the full Phase 5 acceptance test (real device signup -> trial -> /me shows tier=closer) can run. Next: web/ Next.js signup flow, mobile login screen.
- **2026-07-13 — First EAS iOS development build queued.** Frederick ran `eas login` (browser-based, his credentials never touched Claude) and `eas build --platform ios --profile development` himself in his own terminal (Apple 2FA + device registration, same reason -- credentials stay with him). EAS project `@danzoysoftware/mobile` created, bundle id `com.tapowner.app`, distribution cert + provisioning profile generated, his iPhone registered as a provisioned device. Build: https://expo.dev/accounts/danzoysoftware/projects/mobile/builds/a22c84dd-7ceb-4d3a-8e3c-4262e299ca9c. Once installed: Expo Go is retired for this app going forward -- use `npx expo start --dev-client` and open from the custom dev client icon on his phone, not Expo Go.
- **2026-07-13 — Apple Developer Program (Individual) ACTIVE.** Approved ~22min after order (unusually fast for individual enrollment). "Welcome to the Apple Developer Program" + "Welcome to App Store Connect" both received. Confirmed: Frederick already has an Expo account (`danzoysoftware`, plus a `danzoysoftwares-team` org) from a prior Expo Go install -- no new signup needed. Next: `eas login`, EAS project config, first iOS development build.
- **2026-07-13 — Apple Developer Program (Individual) enrollment submitted.** Order W1591399428, $99.00, "being processed." LLC not yet filed, so Individual (not Organization) per Frederick's decision — accepted tradeoff: this ties the account to him personally; migrating to an org account happens later if/when vertical #2 needs shared infra under the umbrella LLC. Waiting on Apple's activation (minutes to 48h typical). Once active: finish EAS project setup, trigger first iOS development build.
- **2026-07-13 — Phase 3 — code complete, typechecked, BLOCKED on Apple Developer enrollment ($99/yr).** Built the map screen: MapLibre (`@maplibre/maplibre-react-native` v11.3.6, real API verified against installed `.d.ts` files, not assumed from docs), OpenFreeMap "liberty" style, GPS blue-dot via `trackUserLocation="default"` + `expo-location` permission gate (exact string from the build doc), parcel boundary `VectorSource`/`Layer` pointed at the Phase 2 tile endpoint, `minzoom=16` per spec. `npx tsc --noEmit` clean. Cannot go further: MapLibre has native code, so it needs a custom EAS dev-client build, not Expo Go — confirmed Expo Go compatibility ends here for the rest of the project. Confirmed (not assumed) via research: EAS cannot sign ANY build for a physical iPhone without a paid Apple Developer Program membership — no free-tier workaround via EAS. The build doc already scheduled this enrollment for "week 2, when LLC exists" (§11 item 4); Phase 3's on-device acceptance test just hit that wall on day 1. Asked Frederick: pay $99/yr now to unblock, or defer on-device testing while more phases' code gets built. Local macOS/Xcode build (his MacBook) is a free stopgap but re-signs every 7 days on a personal Apple ID — not suggested as the real path, just noted as an option.
- **2026-07-12 — Gotcha: Railway CLI's "linked service" silently changed.** Running `railway service link db` during Phase 1 (to attach the volume) silently changed which service subsequent unscoped CLI commands (`railway deployment list`, `railway logs`) target -- caused ~10 min of debugging a "phantom" deploy failure that was actually me reading the `db` service's deploy history, not `api`'s. Also: `git push` to the GitHub-connected api service did NOT auto-trigger a rebuild (webhook didn't fire, cause unconfirmed), and `railway redeploy` only restarts the *previous* build artifact rather than rebuilding from new source. Fix: always pass `--service <name>` explicitly on every Railway CLI call; to force a real rebuild from current code, use `railway up --service api --ci` from the repo root (Dockerfile context), not `redeploy`.
- **2026-07-12 — Phase 2 — local verification PASSED.** `GET /parcels/at?lat=&lng=` and `GET /tiles/{z}/{x}/{y}.mvt` built (Fastify + node-postgres). Point lookup: tested against one real, verified parcel per county (Bexar/Dallas/Harris/Tarrant/Travis) — all 5 owners matched exactly. Tile endpoint: z<15 correctly rejected with 204 before hitting the DB; a real z16 tile over downtown San Antonio returned valid MVT bytes (38,023 bytes) on first request (cache miss, 397ms DB query, logged), byte-identical on second request (cache hit, no DB round trip, logged) — cache proof satisfied via LRU cache (lru-cache, max 2000 tiles, 1h TTL). Rate limiting live via @fastify/rate-limit, per-IP (`x-ratelimit-*` headers confirmed), 100 req/min default (env-overridable; per-user layer deferred to Phase 5 when sessions exist). Input validation: missing/out-of-range lat-lng → 400, no parcel at point → 404 (not a fabricated empty success), bad tile z/x/y → 400. Next: deploy to Railway and re-verify against the live URL to fully close Phase 2's acceptance test.
- **2026-07-12 — Phase 1 — PASSED ✅ (5/5).** Spot-check: Frederick's own home, 2807 Stokely Hl, San Antonio TX 78258 (Bexar, id 542731, apn 16334-040-0180) → owner "DANZOY FREDERICK & MELISSA A", year_built 2006, lot_size_sqft 15,370.18, assessed_total_value $779,680 — all correct. living_area_sqft/has_pool/last_sale_price cleanly null as expected (StratMap doesn't carry them, logged above). Notable: mailing_address shows "2807 STOKEY HL" (missing an L) — a typo in Bexar CAD's own source record, copied verbatim, not introduced by the pipeline; confirms fields aren't being silently "cleaned." Next: Phase 2 (Parcel API).
- **2026-07-12 — Phase 1 — 4/5 acceptance criteria PASSED, 1 pending Frederick.** count(*) = 4,513,276 (millions ✅). Bexar 709,541 / Harris 1,523,641 / Dallas 694,160 / Travis 828,773 / Tarrant 757,161 all present ✅. Point-in-polygon round-trip test: picked a real Bexar parcel (id 197374, owner MCNEAL WILLIE M ETAL), queried by its own centroid point, got the exact same parcel back — GIST index used (`Index Scan using parcels_geom_gist`), 54.36ms, well under the 100ms bar ✅. Protected-record heuristic renders correctly (owner_name null → is_protected=true) ✅. Remaining: "spot-checked known property" needs Frederick's known San Antonio address — see next message. Note: living_area_sqft/has_pool/last_sale_price will be null for whatever property he picks, expected per the StratMap-schema-gap finding above, not a bug.
- **2026-07-12 — Bug found+fixed: null-geometry crash.** `to_multipolygon_wkt()` called shapely's `shape()` before checking for a null geometry, crashing on counties that have parcels with no geometry at all (Bexar happened to have zero; Travis and Tarrant both do). Transactional per-county design caught this correctly — both failed loads rolled back with zero orphaned rows, no data corruption. Fixed by returning `None` (row skipped) before the `shape()` call when geometry is null.
- **2026-07-12 — Expo pinned to SDK 54** (`expo@^54`, RN 0.81.5), matching Expo Go 54.0.2 on Frederick's iPhone (his App Store serves nothing newer). create-expo-app scaffolded SDK 57 → "incompatible version" in Expo Go. Also removed the SDK 57 template's `plugins: ["expo-status-bar"]` from app.json (crashes `expo start` on SDK 54). Revisit SDK upgrade when we move to EAS dev builds (Phase 10) — EAS builds don't depend on Expo Go's supported SDK.
